generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "sqlite"
}

model Playlist {
  id     Int    @id @default(autoincrement())
  userId String
  name   String @unique()
  musics String
}
